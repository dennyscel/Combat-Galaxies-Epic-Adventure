name: ECHOLOOM Passada 1 Legacy Transfer Check

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Workflow legado desativado. Use echoloom-passada1-ci-final.yml."
